# B 题三任务解决方案与实验计划

> 选题：复赛 B「从燃油到纯电：电动汽车消费者转型分析」  
> 数据：`复赛B题/B题数据集.csv`  
> 题面：`复赛B题/复赛B：从燃油到纯电：电动汽车消费者转型分析.pdf`  
> 制订日期：2026-08-30  
> 状态：建模前规划，需在取得正式测试集说明后冻结最终特征边界

## 1. 总体目标与原则

使用 50,000 条、23 列的表格数据完成三个回归任务：

| 任务 | 预测目标 | 主评分 |
|---|---|---|
| T1 月度能源消耗量预测 | `monthly_energy_consumption_kwh` | 调整决定系数 adj-R²₁ |
| T2 月度充电成本预测 | `monthly_charging_cost` | 调整决定系数 adj-R²₂ |
| T3 燃油替代支出预测 | `fuel_expense_per_month` | 调整决定系数 adj-R²₃ |

执行原则：

1. 先做可解释的公式/线性基线，再做非线性模型；复杂模型只有在严格 OOF 验证中稳定提升才晋级。
2. 所有填补、编码、截断、特征选择和级联预测均在训练折内拟合，禁止把验证折或测试集信息带入训练。
3. 三个目标列不能因为同时出现在训练 CSV 中就互相随意使用；是否可用取决于正式测试时该字段是否真实可得。
4. 主结果采用严格端到端口径；使用真实中间目标得到的成绩只能标为 Oracle（理论上界）。
5. 本题仅 5 万行、低维表格数据，核心方案以 CPU 为主。在线平台用于重复交叉验证、批量调参与结果复现，不租用高端 GPU。

## 2. 数据基础评估

### 2.1 数据结构

- 规模：50,000 行 × 23 列。
- 类型：19 个数值列、4 个类别列。
- 类别列：
  - `education_level`：High School / Bachelor / Master / PhD
  - `city_type`：Urban / Suburban / Rural
  - `current_vehicle_type`：Hatchback / Sedan / SUV / Truck
  - `ev_adoption_likelihood`：Low / Medium / High
- 无完整重复行、无预测特征重复行、无无穷值、无常量列。
- 当前文件三个目标均有真值；当前目录未见独立正式测试集。

### 2.2 缺失情况

| 字段 | 缺失数 | 缺失率 | 默认处理 |
|---|---:|---:|---|
| `education_level` | 500 | 1% | 训练折内填为独立类别 `Missing` |
| `charging_station_accessibility` | 500 | 1% | 训练折中位数填补，消融缺失指示器 |
| `ev_knowledge_score` | 500 | 1% | 训练折中位数填补，消融缺失指示器 |

三个缺失模式近似随机，共影响 1,489 行。不能为图省事直接删行；填补器必须放入交叉验证 Pipeline。

### 2.3 目标分布与异常

| 目标 | 均值 ± 标准差 | P1 / 中位数 / P99 | 范围 |
|---|---:|---:|---:|
| 月能耗 | 186.74 ± 88.25 | 24.2 / 179.3 / 427.6 | 15.6–645.9 |
| 充电成本 | 40.235 ± 24.940 | 4.1 / 35.2 / 116.5 | 1.4–199.6 |
| 燃油支出 | 295.17 ± 130.18 | 16.7 / 294.8 / 604.3 | -99.7–862.0 |

异常处理原则：

- `fuel_expense_per_month` 有 271 个负值（0.542%）。它们业务上异常，但符合当前数据的加性噪声机制；评分模型默认保留，不删行、不取对数、不截断预测。
- `annual_income` 右偏，最大 250,000；只把 `log1p` 和训练折 99.5% winsor 作为消融，不默认删除高收入样本。
- 日通勤最大 97.9 km、周里程最大 732.3 km，属于重要目标信号，不按 IQR 机械删除或截断。
- 电价真实范围为 0.08–0.35，共 28 个离散值，边界值不是异常。
- 所有目标保留原量纲。若做目标变换，必须证明逆变换后的 OOF adj-R² 提升。

### 2.4 已发现的统计机制

关键 Pearson 相关：

| 目标 | 最强关系 |
|---|---|
| T1 月能耗 | 周里程 0.9361；日通勤 0.8850 |
| T2 充电成本 | 月能耗 0.7651；周里程 0.7155；电价 0.5839 |
| T3 燃油支出 | 日通勤 0.9515；周里程 0.8989 |

高置信候选生成机制：

```text
weekly_distance ≈ daily_commute × U(5, 8)
energy ≈ weekly_distance × 4.3 × U(0.14, 0.24)
fuel ≈ 8.4 × daily_commute + N(0, 40²)
charging_cost ≈ energy × 未舍入电价
```

含义：

- T1 中未观测能效近似独立随机量，复杂模型未必能突破约 0.876 的 OOF R²。
- T2 的核心不是无约束机器学习，而是「预测能耗 × 电价」的级联公式。
- T3 的主要可预测部分几乎由日通勤里程提供，剩余约 40 的标准差接近随机噪声。
- 题面提示的车型/地域交互必须实验并在报告中说明，但当前数据上尚无稳定增益证据。

## 3. 待组委会确认的口径

正式建模前必须书面确认：

1. 三个任务是否使用同一测试表，以及每个任务测试表中哪些目标列被隐藏。
2. T2 测试时是否提供真实 `monthly_energy_consumption_kwh` 和 `fuel_expense_per_month`。
3. adj-R² 中 `p` 对 One-Hot、树模型、公式级联模型如何计算。
4. 预测结果 CSV 为何要求包含“真值”；正式盲测提交时真值列应留空、由组委会填充，还是赛后提交。
5. 是否允许三个任务之间使用预测值级联，以及是否要求分别提交独立模型。

确认前采用最保守且可部署的严格口径。

## 4. 特征可用性与泄露边界

| 任务 | 默认安全特征 | 默认排除/禁止 |
|---|---|---|
| T1 | 除三个目标外的原始前置特征 | `monthly_energy_consumption_kwh`；`monthly_charging_cost` 或成本/电价重构能耗；样本行号 |
| T2 | 原始安全特征、T1 严格 OOF/测试预测、电价及交互 | 训练用真实能耗而测试用预测能耗；目标本身；全量拟合产生的训练内预测 |
| T3 | 出行、车型、城市、人口统计等前置特征 | 目标本身；默认排除真实能耗、充电成本等转型后结果；样本行号 |

补充规则：

- T1 的燃油支出、T2 的真实燃油支出只有在测试时确实提供才可列为条件特征；主方案先排除，避免字段缺失和跨任务泄露。
- T1 用 `monthly_charging_cost / electricity_cost_per_kwh` 可获得近乎真实能耗，是明确的下游目标泄露。
- T2 不能在训练集使用真实能耗、测试集改用预测能耗，这会造成 teacher forcing 和严重的训练/推理分布错位。
- T3 不使用真实能耗和充电成本；它们既不符合“基于当前出行习惯和车辆属性”的前置语义，实测也没有增量收益。
- `ev_adoption_likelihood` 可能是由多项态度变量聚合产生的后置评分；先作为可疑特征单独消融，不默认纳入最小模型。

## 5. 公共预处理方案

### 5.1 数据校验

每次训练前自动检查：

1. 文件 SHA-256、行数、列名、数据类型和目标是否存在。
2. 重复行、缺失率、无穷值、非法类别、数值边界和行顺序。
3. 生成稳定 `row_id` 仅用于 OOF/提交回填，绝不入模。
4. 原始 CSV 只读，所有中间结果写入独立 `artifacts/`。

### 5.2 编码与缩放

- 线性模型：类别列 One-Hot（drop first），数值列中位数填补；Ridge/Huber 前标准化。
- CatBoost：保留原始类别，缺失填 `Missing`；数值缺失由训练折中位数或模型原生处理。
- LightGBM/XGBoost：类别统一编码，编码器只在训练折拟合。
- `education_level` 和 `ev_adoption_likelihood` 同时测试 One-Hot 与有序编码；只有稳定提升才用有序假设。
- 距离、成本、收入的原量纲版本始终保留一组基线，防止变换破坏近似线性公式。

## 6. 验证与模型晋级标准

### 6.1 交叉验证

- 快速筛选：固定随机种子的 3-Fold shuffled CV。
- 候选确认：5-Fold shuffled CV × 3 个种子；三个任务共用同一组外层 `fold_id`。
- T2 级联必须使用嵌套 OOF，详见第 8 节。
- 最终报告使用全部样本拼接后的 pooled OOF 预测计算一次主指标，不把各折 adj-R² 简单平均。

```text
R² = 1 - Σ(y - ŷ)² / Σ(y - mean(y))²
adj-R² = 1 - (1 - R²) × (n - 1) / (n - p - 1)
```

`p` 暂定口径：

- 线性模型：实际进入模型的编码后预测变量数，不含截距。
- 公式模型：报告原始独立输入数和派生项数；T2 严格级联保守记 `p_pipeline=2`（周里程、电价），并另报元模型输入数。
- 树模型：不存在公认的线性自由度定义；同时报告普通 R²、输入特征数 `p_input` 和按 `p_input` 计算的参考 adj-R²，并等待官方确认。

### 6.2 辅助指标与切片

每次实验同时记录：R²、adj-R²、RMSE、MAE、各折均值/标准差、运行时间和峰值内存。额外评估：

- 日/周里程顶部 1% 与底部 1% 的 RMSE。
- 各车型、城市类型的分组误差和残差偏差。
- T3 负真值样本、低通勤样本的误差。
- T2 不同电价档位的残差。

### 6.3 晋级门槛

复杂模型或新增特征需同时满足：

1. 相对当前最佳模型 pooled OOF ΔR² ≥ 0.0002。
2. 配对 Bootstrap 的 95% 增益区间下界大于 0。
3. 至少 2/3 个重复种子提升。
4. 尾部和关键分组误差无明显恶化。
5. adj-R² 的增益足以抵消新增特征数惩罚。

## 7. T1：月度能源消耗量预测

### 7.1 数据基础与清洗

- 核心变量是 `weekly_travel_distance_km`；单变量线性 OOF R²≈0.8763。
- `daily_commute_km` 与周里程高度相关，二者同时进入线性模型时检查 VIF/条件数；adj-R² 无提升则仅留周里程。
- 不删除长距离样本；它们是模型识别高能耗的关键。
- 缺失字段与核心关系较弱，按公共方案折内填补即可。

### 7.2 候选特征块

| 特征块 | 内容 | 用途 |
|---|---|---|
| F0 核心 | 周里程 | 最小物理/线性基线 |
| F1 行程结构 | 日通勤、`weekly/daily`、`weekly-5×daily`、估算月里程 | 判断非通勤出行是否解释能效 |
| F2 官方交互 | 周里程×车型、日通勤×车型 | 回应题面难点并做消融 |
| F3 车辆/地域 | 车型、车龄、城市及其与里程交互 | 检验能效差异 |
| F4 其他信息 | 人口统计、设施、态度变量 | 整块加入/删除，防止噪声堆积 |

`估算月里程` 与周里程是常数倍关系，不能同时作为两个独立线性特征增加 `p`。

### 7.3 模型顺序

1. P0：训练折内比率均值 `ŷ = weekly × mean(y/weekly)`、过原点 OLS、带截距 OLS。
2. P1：Ridge、Huber、低自由度 `SplineTransformer + Ridge`。
3. P2：浅层 CatBoost / LightGBM / XGBoost；优先拟合 P0 残差，而非直接用全部特征硬拟合目标。
4. P3：P0 与最佳非线性模型做 OOF 加权融合，权重只根据 OOF 确定。

树模型限制：深度 3–6、较小学习率、early stopping；不做深度学习。

### 7.4 实验路径

1. 比较三个 P0 公式/OLS 基线，冻结最优核心模型。
2. 逐块加入 F1、F2、F3、F4，每次仅改变一个特征块。
3. 画核心模型残差与所有安全特征的关系；只有残差存在稳定结构才启用 P2。
4. 用 Permutation Importance 与分组消融解释模型；SHAP 只对最佳树模型抽样分析。
5. 若复杂模型不越过晋级门槛，最终提交稀疏模型，以更高 adj-R² 和更低过拟合风险为优先。

预期：合法端到端 R² 约 0.876；车型交互当前实测无增益，但应作为报告中的负结果保留。

## 8. T2：月度充电成本预测

### 8.1 数据基础与核心公式

```text
cost_physics = energy_hat × electricity_cost_per_kwh
```

- 使用真实能耗计算，R²≈0.99943、RMSE≈0.60，只能作为 Oracle 上界，除非组委会确认测试时提供真实能耗。
- 使用严格 T1 OOF 能耗，R²≈0.9178；主要误差来自 T1 无法预测的个体能效，而不是成本模型本身。
- 电价 0.08–0.35 均合法，不做 winsor 或边界裁剪。

### 8.2 严格嵌套 OOF 级联

对每个 T2 外层折执行：

1. 将数据分为外层训练折 A 和验证折 B。
2. 只在 A 内再次交叉验证 T1，为 A 中每行生成内层 OOF `energy_hat`。
3. 用 A 的全部数据重训 T1，预测 B 的 `energy_hat`。
4. A 使用内层 OOF 能耗，B 使用外层预测能耗，分别构造 `cost_physics`。
5. 在 A 训练 T2 校准/残差模型，在 B 评估。
6. 汇总所有外层 B 得到最终 T2 OOF 预测。

禁止先在全量数据生成一份 T1 OOF，再直接用于 T2 外层 CV；那样外层训练过程可能间接看到验证折信息。

最终测试推理：

1. 全训练集生成 T1 OOF，供最终 T2 模型训练。
2. 每个 T1 折模型预测测试集，取均值得到测试 `energy_hat`。
3. 构造测试 `cost_physics`，由最终 T2 模型预测成本。

### 8.3 模型与实验顺序

1. P0：直接提交 `cost_physics`。
2. P1：线性校准 `cost ~ cost_physics + energy_hat + electricity_rate`。
3. P2：Ridge 加入周里程、电价交互、日通勤和官方建议变量。
4. P3：浅层 CatBoost/LightGBM 预测 `真实成本 - cost_physics` 残差。
5. P4：公式与残差模型按 OOF 最优权重融合。

必须做的对照：

| 实验 | 目的 |
|---|---|
| 不使用能耗级联 | 普通模型基线 |
| 严格嵌套 OOF T1→T2 | 主结果 |
| 真实能耗×电价 | Oracle 上界，不参与严格排名 |
| 真实训练能耗、测试预测能耗 | 错误示范，用于说明为何禁止 |
| 加/不加燃油支出或其预测值 | 验证官方建议变量是否有增益及是否可部署 |

如果测试不提供燃油真值，只能删除该特征或使用同样严格产生的 T3 OOF/测试预测；当前证据显示它不是必要特征。

## 9. T3：燃油替代支出预测

### 9.1 数据基础与清洗

- 核心变量是 `daily_commute_km`；单变量 OLS OOF R²≈0.9054。
- 近似关系为 `fuel ≈ 8.4 × daily_commute + N(0,40²)`。
- 保留 271 个负标签和全部高里程样本；默认不对目标取对数、不删除负值、不做目标 winsor、不把预测强制截为 0。
- 同时输出一个非负业务解释版本可供图表使用，但评分提交使用未经截断且 OOF 更优的版本。

### 9.2 候选特征块

| 特征块 | 内容 |
|---|---|
| F0 核心 | 日通勤里程 |
| F1 行程结构 | 周里程、周/日比、估算月里程、非通勤里程 |
| F2 官方交互 | 日通勤×车型、日通勤×城市、日通勤×车型×城市 |
| F3 车辆/地域代理 | 车型、车龄、城市、`log1p(annual_income)` |
| F4 其他前置特征 | 人口统计、设施和态度变量整块消融 |

### 9.3 模型顺序

1. P0：带截距 OLS、过原点 OLS、Huber。
2. P1：Ridge 和日通勤低自由度样条；控制周里程共线性。
3. P2：加入官方车型/地域交互的稀疏线性模型。
4. P3：在最佳 OLS 残差上训练浅层 CatBoost/LightGBM/XGBoost。
5. P4：OLS 与最佳残差模型做 OOF 融合。

### 9.4 必做消融

- 负目标：保留 / 删除 / winsor / Huber 四组对照。
- 核心变量：日通勤单变量 / 加周里程 / 加行程结构。
- 官方提示：车型交互 / 地域交互 / 三阶交互。
- 模型：原目标树模型 / OLS 残差树模型。

当前对照显示：原始标签 OLS≈0.90541，删除负值或 winsor 后略降；车型和地域交互也未提升。因此最终模型很可能仍是稀疏 OLS。

## 10. 在线算力平台方案

### 10.1 资源判断

本题不需要 A100/4090，也不需要深度学习。推荐资源：

```text
CPU：4–8 vCPU
内存：16 GB
磁盘：10–20 GB SSD
GPU：默认不需要；批量 CatBoost/XGBoost 时 T4 或 RTX 3060 12 GB 已足够
```

建议平台优先级：

| 平台 | 用途 | 使用建议 |
|---|---|---|
| Kaggle Notebook | 免费 CPU CV、共享复现实验 | 首选；数据集设为 Private，结果及时下载 |
| Google Colab | 临时验证、团队协作备份 | 免费资源不保证，运行结果同步到 Drive |
| AutoDL 等国内租用平台 | 免费平台不稳定或最终重复 CV | 租最低满足 4–8 核/16 GB 的实例；需要 GPU 时选 T4/3060，不租高端卡 |

平台额度、价格和实例型号会变化，启动前以平台页面为准。付费预算建议先设 20–50 元硬上限，只有通过 3 折筛选的模型才能进入付费重复验证。

### 10.2 环境（uv 管理）

本项目统一使用 [uv](https://docs.astral.sh/uv/) 管理 Python 与依赖，开发、在线平台、提交复现三套环境保持一致。

```text
Python 3.11
pandas, numpy, scipy, scikit-learn
catboost, lightgbm, xgboost
optuna, shap, pyarrow, joblib
```

uv 初始化与日常命令：

```bash
uv init                          # 生成 pyproject.toml
uv python pin 3.11               # 固定解释器，写入 .python-version
uv add pandas numpy scipy scikit-learn catboost lightgbm xgboost optuna shap pyarrow joblib
uv lock                          # 锁定依赖，生成 uv.lock
uv sync                          # 按锁文件安装到 .venv
uv run <script.py>               # 在项目环境内运行脚本
```

环境约定：

- 依赖只通过 `uv add` 变更，`uv.lock` 随代码一起版本管理；不手写 `requirements.txt`，若平台需要可导出 `uv export --format requirements-txt`。
- `.venv/` 加入 `.gitignore`，任何平台重新 `uv sync` 即可复现，不提交或同步虚拟环境。
- `uv sync` 后执行 `uv pip check` 校验依赖一致；三平台首次运行前各跑一次 Schema 冒烟测试确认可复现。
- 固定随机种子、折号和线程数。
- Optuna 并行与模型 `n_jobs` 只启用一层，避免线程过度竞争。
- 单模型先 20–30 次 Optuna 试验，启用 early stopping 和 pruner。
- 只有每任务前两名进入 5 折×3 种子确认。

### 10.3 数据与结果同步

1. 原始 CSV 本地计算 SHA-256；上传后再次校验。
2. 代码与配置通过私有 Git 仓库同步；不提交原始数据、环境目录和平台密钥。
3. Kaggle 使用 Private Dataset，Colab 使用 Drive，AutoDL 使用 SCP/对象存储。
4. 每批试验回传配置、指标、日志、逐行 OOF、模型和最终预测。
5. 在线实例训练完成立即停止/关机；不要把唯一结果留在临时系统盘。
6. 最终预测至少保留 6 位小数，不按训练标签的一位小数提前四舍五入。

### 10.4 建议运行分配

| 环节 | 运行位置 | 原因 |
|---|---|---|
| Schema 检查、EDA、OLS/Ridge、3 折筛选 | 本机或 Kaggle CPU | 内存和算力需求很低 |
| CatBoost/LightGBM 20–30 次调参 | Kaggle CPU；必要时 AutoDL | 批量任务耗时但无需高端 GPU |
| T2 嵌套 OOF、5 折×3 种子 | AutoDL 4–8 核或 Kaggle | 适合连续稳定运行 |
| SHAP | 最佳树模型抽样 2,000–5,000 行 | 避免无意义全量计算 |
| 全量重训和提交生成 | 与最终验证相同环境 | 减少环境差异 |

## 11. 实验管理与可复现性

建议目录：

```text
pyproject.toml        # uv 项目配置与依赖声明
.python-version       # 固定 Python 版本
uv.lock               # 锁定的依赖版本
configs/              # 每次实验的 YAML/JSON 配置
src/                  # 数据、特征、模型、指标和推理代码
notebooks/            # 只做 EDA 与结果展示
artifacts/folds/      # 固定 fold_id
artifacts/oof/        # 每次实验逐行 OOF
artifacts/models/     # 最终候选模型
artifacts/metrics/    # runs.csv / JSON
submissions/          # 三任务预测文件
reports/figures/      # 论文图表
```

每个实验至少记录：

```text
run_id, data_sha256, code_commit, target, leakage_mode,
fold_hash, seed, feature_set, p_raw, p_encoded,
model, params, pooled_r2, adj_r2, rmse, mae,
fold_mean, fold_std, tail_rmse, runtime_sec, peak_ram,
oof_prediction_path, notes
```

实验纪律：

1. 所有候选模型必须保存逐行 OOF；融合权重只能基于 OOF。
2. 特征消融每次只改变一个特征块。
3. 调参 CV 与最终报告 CV 分离，最终报告折在冻结方案后运行。
4. 不以单个折或单个随机种子的偶然提升作结论。
5. 模型重要性以折外 Permutation Importance 和特征块消融为主；相关特征下的 SHAP 仅作辅助。

## 12. 总体实验流程与退出条件

| 阶段 | 工作 | 产物/退出条件 |
|---|---|---|
| P0 口径冻结 | 确认测试字段、adj-R² 的 `p`、级联规则 | 泄露清单和提交格式书面固定 |
| P1 数据管线 | Schema 校验、折内清洗、固定 fold | 相同配置可复现同一 OOF |
| P2 稀疏基线 | T1/T3 公式与 OLS；T2 严格公式级联 | 三任务基线和残差诊断完成 |
| P3 特征消融 | 官方交互、行程结构、可疑特征块、异常策略 | 冻结最小有效特征集 |
| P4 非线性候选 | 仅对有残差信号的任务训练浅层 Boosting | 候选通过晋级门槛或被淘汰 |
| P5 严格级联 | 嵌套 T1→T2；Oracle 只作上界 | 无 teacher forcing、无跨折泄露 |
| P6 最终确认 | 5 折×3 种子、Bootstrap、必要时 OOF 融合 | 每任务冻结 1 个主模型和 1 个备选 |
| P7 全量推理 | 全量重训、测试预测、行号/列名/精度检查 | 三份可提交 CSV 和模型包 |
| P8 报告归档 | 特征重要性、消融、调参、泄露说明、AI 声明 | 论文、代码、日志与支撑材料一致 |

首轮优先实验：

1. 固定 5 折并复现 T1 周里程 OLS、T2 严格级联、T3 日通勤 OLS。
2. 验证官方要求的 T1 车型交互和 T3 车型/地域交互。
3. 完成 T3 负目标四种策略消融。
4. 检查三个基线残差是否存在可被其他特征稳定解释的结构。
5. 只有第 4 步为真，才租用在线算力进行 Boosting/Optuna。

## 13. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 正式测试字段与训练表不同 | 模型无法推理或产生泄露 | P0 先确认；保留严格与条件两套特征配置 |
| adj-R² 的 `p` 定义不明确 | 模型排名判断错误 | 同时报普通 R²、不同 `p` 口径，并向组委会确认 |
| 合成数据的随机噪声不可预测 | 复杂模型过拟合、线上下降 | 稀疏公式优先；重复 OOF 与 Bootstrap 晋级 |
| T2 teacher forcing | 线下虚高、线上崩溃 | 嵌套 OOF；Oracle 与主结果明确分开 |
| 机械删除异常值 | 丢失高里程信号并降低 R² | 所有异常策略必须以 OOF 消融决定 |
| 在线实例中断/临时盘释放 | 结果丢失 | 每折或每批试验同步 OOF、配置与日志 |
| 无证据的大规模调参 | 浪费租用成本 | 3 折筛选、20–30 trials、early stopping、硬预算 |

## 14. AI 使用合规

竞赛规定 AI 只能作为辅助工具，核心建模、代码实现、结果复核和结论必须由队员独立完成。本计划及后续 AI 辅助内容必须：

1. 保存重要提示词、原始回复摘要、采用内容、人工修改和复核记录。
2. 在论文参考文献后附“AI 工具使用声明”，相应正文位置按规定标注。
3. 在支撑材料中提交 `队伍编号 AI 使用说明.pdf`。
4. AI 辅助代码在文件头或函数处添加规定注释；队员必须重写/审查并亲自运行验证。
5. 不直接提交 AI 生成文本，避免 AIGC 比例超过规定阈值；结论必须由实际实验支撑。

本文件应记入 AI 使用日志：

```text
工具：OpenAI GPT-5.6-sol（通过 OpenCode）
日期：2026-08-30
环节：数据审计思路、模型候选、交叉验证与在线算力实验规划
提示词：用户本次关于“三任务解决方法、数据清洗、模型、技术路径、实验流程与在线算力”的原始请求
采纳要求：由队员逐项核对数据统计、重新实现并独立运行实验后方可用于参赛材料
```
